# ========== pnpm 11 .npmrc：仅用于 registry / auth 配置 ==========
# 其他配置（hoist / builds / storeDir 等）已迁移到 pnpm-workspace.yaml
# 环境变量前缀也从 npm_config_* 改为 pnpm_config_*（pnpm 11 起）

# 国内默认走 npmmirror；海外或私有仓库可在 CI 覆盖环境变量 PNPM_CONFIG_REGISTRY
registry=https://registry.npmmirror.com
